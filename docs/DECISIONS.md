# 产品决策日志（Decision Log）

> 由 Orchestrator 维护。**所有角色以本日志为准**：前端、后端、API、测试、文档引用任何与下述决策冲突的内容时，一律以本日志为准并指出冲突。
> 变更须走 PR（Orchestrator 审查）。

## 已拍板决策（用户确认，2026-08-24）

| # | 决策点 | 决策 | 影响范围 |
|---|--------|------|----------|
| D1 | 支付方式 | **模拟支付**：无真实支付渠道，「支付成功」即模拟入账；订单/托管/结算**状态机必须真实完整**（pending payment → paid(escrow) → delivered → completed / refunded 等） | 后端支付模块、钱包 |
| D2 | 货币单位 | **平台币 Credits（CR）**：整数，1 单位最小粒度；界面显示 `CR 500` 或 `500 CR`（统一用 `500 CR` 格式，tabular-nums）；**不使用** USD/CNY/¥ 符号；设计文档中的 ¥/$ 示例均为占位，实现一律换为 CR | 全局文案、金额展示、API 字段 |
| D3 | 注册验证 | **邮箱 + 密码注册，免邮箱验证邮件**（演示环境）；密码最小长度 8 | 认证模块 |
| D4 | 角色模型 | 注册时选择**主角色**；一个账号可**并存三种角色**（buyer/seller/contractor），个人中心可查看/切换；API 用角色集合表达（如 `roles: ["buyer","seller"]`） | 用户模型、权限、导航 |

## 默认采纳的假设（PM 提出，用户未反对，先行实现；如要改随时提）

| # | 决策点 | 默认值 | 备注 |
|---|--------|--------|------|
| A1 | 大额充值阈值 | 单次充值 ≥ **100 CR** 需二次确认（显示充值后余额变化）；阈值集中配置 | 设计文档 §5.2 的 ¥500 以此为准 |
| A2 | 退款窗口 | 作品订单 **14 天**（未确认收货前可申请退款） | |
| A3 | 托管放款 | 买家确认收货即放款；接单类验收期 **7 天**自动确认 | |
| A4 | 提现到账时间 | **1–3 个工作日**，页面必须明示 | |
| A5 | 手续费 | 下单显示含手续费总价；费率默认 **5%**（集中配置） | 与设计系统「下单前一屏显示总价」配套 |
| A6 | 演示数据 | 系统内置演示数据（示例作品、示例需求），保证空库可演示 | 配合空态设计 |

## 状态词汇表引用

业务状态唯一事实来源：`docs/STATUS_VOCABULARY.md`。任何新状态必须先入词汇表再实现。
